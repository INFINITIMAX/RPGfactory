# RPG Factory runs locally. Never commit the real .env file.
# Optional: absolute Pi run-artifact roots separated by the platform path delimiter.
# PI_SUBAGENTS_ROOTS=
# Optional: one absolute Pi mission-store directory for the read-only mission board.
# PI_SUBAGENTS_MISSION_ROOT=

PORT=5311
